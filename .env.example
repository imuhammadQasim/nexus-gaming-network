# Copy to .env and fill in real values. Never commit .env.

# Supabase (BaaS) — from Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Public site URL — used as the referral-link fallback during SSR
NEXT_PUBLIC_SITE_URL=https://apexplay.example.com
